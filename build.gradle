plugins {
    id 'base'
}

allprojects {
    group = 'de.selectiverender.plots'
    version = providers.gradleProperty('addon_version').get()
}

tasks.named('assemble') {
    dependsOn(':paper:assemble', ':fabric:assemble')
}

tasks.named('check') {
    dependsOn(':paper:check', ':fabric:check')
}
